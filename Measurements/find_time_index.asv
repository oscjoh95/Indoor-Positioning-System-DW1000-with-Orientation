function [index,Turn] = find_time_index(time,trueTimes)
    i=2;
    index = 2;
    trueTimes = trueTimes-truetimes(1);
    while(time>trueTimes(i))
        index = index+2;
        i=i+2;
        if time>trueTimes(i)
            time>trueTimes(i)
        end
    end
end